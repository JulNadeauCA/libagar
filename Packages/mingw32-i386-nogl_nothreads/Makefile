
PLATFORM=	unix
OS=		mingw32
ARCH=		i386
FLAVOR=		-nogl_nothreads
CONFIGURE_ARGS=	--disable-threads \
		--disable-network \
		--without-gl \
		--with-freetype \
		--without-jpeg

include ../Makefile.inc
