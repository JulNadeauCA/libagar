
- attempt to predict which tiles must be redrawn from scrolling.
- character destroys must scan the whole map; use back references.
- move to <sys/queue.h>.
- fix aref index functions (overhead).
- double check that map locks are not held long.
- document the engine.

