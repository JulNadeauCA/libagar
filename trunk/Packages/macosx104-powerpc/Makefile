
PLATFORM=	unix
OS=		macosx
ARCH=		powerpc
FLAVOR=
PORTNAME=	macosx104-powerpc${FLAVOR}
PKGNAME=	agar-${VERSION}-${PORTNAME}

CONFIGURE_ARGS=	--disable-threads \
		--enable-network \
		--with-gl \
		--with-freetype \
		--without-jpeg

include ../Makefile.inc
