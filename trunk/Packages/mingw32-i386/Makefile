
PLATFORM=	unix
OS=		mingw32
ARCH=		i386

FLAVOR=
CONFIGURE_ARGS=	--enable-threads \
		--disable-network \
		--with-gl \
		--with-freetype \
		--without-jpeg \
		--includes=link 

include ../Makefile.inc
